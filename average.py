class average:
    sum = 0
    def setAverage(arr, size):
        sum
        print("Values to be summed: ",arr)
        for i in range(len(arr)):
            int(sum = sum + arr[i])
        return arr
        average = sum/size
        return average

    def main(self):
        size
        arr = []
        size = int(input("how big is your data set? "))
        for i in range(size):
            arr[i] = input("enter a number ")   
        self.setAverage(arr, size)
        print("the average is ", average)
